import { FilterOnIdPipe } from './filter-on-id.pipe';

describe('FilterOnIdPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOnIdPipe();
    expect(pipe).toBeTruthy();
  });
});
